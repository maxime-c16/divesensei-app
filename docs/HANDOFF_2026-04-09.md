# DiveSensei Handoff - 2026-04-09

Branch: `feat/mobile-capacitor-scaffold`

This document is a working handoff for the next Codex session. It captures the current detector/evaluation state, the important session outputs, the measured conclusions, and the safest next starting point.

## Current architecture

The active detector path remains:

`audio -> mel/PCEN -> proposal detection -> engineered features -> logistic regression`

Do not switch model family.

## Major repo state already implemented

- `divesensei evaluate-session` exists and is now the evaluation entrypoint.
- Evaluation sessions are audio-first.
- Session audio is cached once and reused.
- Review proxy generation is separate.
- Evaluation review supports:
  - `dive`
  - `non_dive`
  - `unsure`
  - `false_negative`
- Reviewed evaluation sessions export:
  - hard negatives
  - reviewed candidates
  - candidate diagnostics
  - false negatives
  - proposal failure attribution
  - comparison summaries
- Desktop evaluation review supports manual false negatives.
- Timeline scrubbing in evaluation mode was fixed to allow free seeking.
- Review proxy generation was fixed for iPhone-style MOV files by mapping playable streams only and preferring `h264_videotoolbox` on this Mac.

## Most important measured conclusions so far

### Classifier story

- Champigny hard-negative retraining materially reduced reviewed false positives.
- INSEP hard-negative retraining also materially reduced candidate burden and replayed reviewed non-dives.
- The classifier direction is validated enough to keep.
- The main remaining bottleneck is now upstream proposal/frontend evidence, not the logistic clip model.

### Champigny story

Reference sessions:
- Baseline reviewed session: `outputs/evaluation_champigny_20260406-labelling`
- Retrained clip-model rerun baseline: `outputs/evaluation_champigny_20260406-retrained-proposal`
- Best consolidation run: `outputs/evaluation_champigny_consolidation_20260408`
- Centered consolidation run: `outputs/evaluation_champigny_consolidation_center_20260409`

Measured conclusions:
- Champigny false negatives split into:
  - `recoverable_split_or_competition: 2`
  - `genuine_weak_frontend_miss: 1`
- Narrow local consolidation was the first upstream proposal change that produced a nearby final proposal for a previously missed fragmented dive.
- Centering refinement tightened that result enough to count as a practical recovery under `+-1.0s` human annotation tolerance:
  - FN timestamp `1403.623904s`
  - nearby final proposal `1402.6510895802357s`
  - offset `-0.9728144267648986s`
- That did not reduce strict exported false negatives because replay/export matching is far stricter than human annotation tolerance.
- Broader grouping and overlap-aware consolidation were regressions.
- Current best Champigny proposal-side branch:
  - narrow local consolidation
  - centering
  - no peak-timestamp grouping
  - no overlap-aware bonus

Champigny interpretation:
- Local consolidation + centering is the best current upstream branch for fragmented misses.
- The one genuine weak frontend miss still needs better proposal primitive quality.

### INSEP story

Reference sessions:
- Reviewed session: `outputs/evaluation_insep_quick_9015_20260409_ui`
- Retrained rerun baseline: `outputs/evaluation_insep_quick_9015_20260409_retrained`
- Frontend persistence run: `outputs/evaluation_insep_quick_9015_20260409_frontendpersist`
- Flux-dominant persistence variant: `outputs/evaluation_insep_quick_9015_20260409_frontendpersist_flux`
- Sustained-noise exception run: `outputs/evaluation_insep_quick_9015_20260409_noiseexception`
- Pattern-bonus run: `outputs/evaluation_insep_quick_9015_20260409_patternbonus`

Reviewed INSEP labels:
- `36` dives
- `35` non-dives
- `7` false negatives

INSEP non-dive subtype mix:
- handling noise dominant
- some whistle
- some board rebound

Measured conclusions:
- All `7` INSEP false negatives are currently `genuine_weak_frontend_miss`.
- Baseline retrained INSEP false negatives are all upstream misses:
  - `no_proposal_generated: 7`
- The frontend persistence integral is the first upstream feature that improved proposal formation for a real INSEP weak-frontend miss:
  - baseline nearby frontend candidates: `0`
  - persistence run nearby frontend candidates: `1`
  - baseline nearby final proposals: `0`
  - persistence run nearby final proposals: `1`
  - one miss moved from `no_proposal_generated` to `ambiguous_case`
- This improvement was stable:
  - candidate count unchanged in the first persistence run
  - FP/min essentially unchanged
  - replay coverage unchanged
- The rescued INSEP miss is at:
  - FN timestamp `398.4226974879982`
  - nearby frontend/final proposal at `397.952s`
- Lowering PCEN weight from `0.6` to `0.2` did not rescue additional misses.
- The remaining INSEP misses split across different blockers:
  - some too weak everywhere
  - one strong candidate case around `114.350347s` is persistence-supported but was blocked by `sustained_noise_reject`
  - one PCEN-heavy / low-flux case around `157.576774s` is still fundamentally weak

### 114.350347s case

This is the best remaining INSEP rescue target.

Measured neighborhood:
- strong tail:
  - `post_flux_ratio = 2.091257333755493`
  - `post_rms_ratio = 2.201663017272949`
- persistence bonus:
  - `0.11025305926025485`
- persistence score:
  - `0.3150087407435853`
- flux ratio:
  - `1.7875218518589633`
- PCEN ratio:
  - `0.8596569170572967`
- local prominence:
  - `4.58563756942749`

Measured sequence of blockers:
1. Initial persistence run:
   - still vetoed by `sustained_noise_reject`
2. Bounded sustained-noise exception:
   - mechanically removed that veto
   - row then failed at `weak_pattern_score`
3. Bounded pattern bonus tested on top of the persistence baseline:
   - safe and local
   - but did not rescue the case because in that branch it still hit `sustained_noise_reject`

Important interpretation:
- The sustained-noise exception worked mechanically and safely.
- The pattern bonus also looked safe.
- The only meaningful next test for this case is to combine those two safe mechanisms in the same branch:
  - persistence integral
  - bounded sustained-noise exception
  - bounded pattern bonus

That combined test has not yet been run.

## Current code paths touched

Main files changed in this workstream:
- `src/divesensei/detection/audio_detector.py`
- `src/divesensei/detection/config.py`
- `src/divesensei/app/session_pipeline.py`
- `src/divesensei/io/media_io.py`
- `src/divesensei/metadata/ui_contract.py`
- `src/divesensei/workflows/evaluate_session.py`
- `src/divesensei/workflows/evaluation_session_support.py`
- `src/divesensei/workflows/export_evaluation_review.py`
- `src/divesensei/workflows/replay_evaluation_review.py`
- `src/divesensei/workflows/compare_evaluation_summaries.py`

Newer experiment flags already exist in the codebase, including:
- frontend persistence integral flags
- bounded sustained-noise exception flags
- bounded persistence-supported pattern bonus flags
- local consolidation / centering flags
- overlap-aware and rescue-policy experiment flags

## Important operational notes

- Do not start multiple heavy analysis jobs in parallel on this Mac.
- Keep long-session experiments serialized.
- The user explicitly stopped `npm run dev` because network is unreliable; do not restart it unless asked.
- Prefer local generated proxy MP4 or cached WAV for reruns when network storage is flaky.
- Replay coverage is often degraded on reruns; always inspect:
  - `mapping_coverage`
  - `median_delta_seconds`
  - and do not overclaim recall when replay coverage is low

## Best current branches by problem type

### For fragmented / split proposal misses
- Use centered narrow local consolidation as the best branch reference.

### For weak frontend misses
- Use the frontend persistence integral branch as the best branch reference.
- INSEP proves this can rescue proposal formation for at least one real weak miss.

## Recommended next starting point for the next Codex session

If continuing INSEP weak-miss work, start from:
- `outputs/evaluation_insep_quick_9015_20260409_frontendpersist`
- then compare with:
  - `outputs/evaluation_insep_quick_9015_20260409_noiseexception`
  - `outputs/evaluation_insep_quick_9015_20260409_patternbonus`

The highest-value next measured experiment is:
- combine the already-safe sustained-noise exception and the already-safe persistence-supported pattern bonus for the `114.350347s`-style case
- run on INSEP only
- verify:
  - whether `114.350347s` finally gets a nearby frontend candidate / final proposal
  - whether handling-noise false positives stay flat

That is the cleanest next continuation point.
