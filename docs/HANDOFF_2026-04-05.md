# AI Handoff (2026-04-05)

## Branch
- `feat/mobile-capacitor-scaffold`

## What was done in this session
- Investigated long-running analysis jobs started from the UI.
- Confirmed prior failure root cause was audio decode timeout on large source:
  - `Audio decode timed out after 180.0s`
- Increased long-session audio decode timeout in profile overrides:
  - `src/divesensei/profiles.py`
  - `audio_decode_timeout_seconds` set to `900.0` for long-session baseline/advanced overrides.
- Improved UI-side job failure reporting to surface traceback-derived errors instead of only generic exit code:
  - `apps/desktop/src/lib/analysis-jobs.ts`
- Investigated local 9GB run bottleneck and confirmed it is front-loaded media decode I/O.

## Current bottleneck diagnosis (confirmed)
- Analysis pipeline logs only `session_start` + `detection_start` for a long time because detection is blocked on full audio decode.
- `decode_audio_mono_s16le(...)` in `src/divesensei/io/media_io.py` runs ffmpeg and waits for completion before moving on.
- For `outputs/Champigny_03-04.mov` (~9.51 GB), measured effective read throughput during decode was ~1.75 MB/s in this environment, projecting to ~1.5h just for decode/read.
- Long run was terminated before handoff to avoid background load.

## Process status at handoff
- No active `divesensei.cli detect` process for the Champigny local run.
- No active ffmpeg child from that run.

## Pending modified files (now intended to be source-of-truth)
- `apps/desktop/src/lib/analysis-jobs.ts`
- `src/divesensei/cli.py`
- `src/divesensei/detection/audio_detector.py`
- `src/divesensei/io/media_io.py`
- `src/divesensei/profiles.py`
- `src/divesensei/workflows/save_audio_label.py`
- `src/divesensei/workflows/train_audio_clip_model.py`
- `src/divesensei/workflows/evaluate_audio_pcen_classifier.py` (new)

## Not included in commit
- `screenshots/` (left untracked intentionally)

## Suggested next steps on Mac (better local I/O)
1. Pull this branch and run desktop UI:
   - `make re`
2. Launch a local-path analysis from UI (avoid NAS-mounted path for first test).
3. If run still feels stalled, verify first-stage decode throughput directly:
   - `ffmpeg -v error -nostdin -threads 1 -i <video> -map 0:a:0? -ac 1 -ar 16000 -f s16le - > /dev/null`
4. Consider next pipeline improvements:
   - Add decode progress heartbeat events to `session_pipeline.log.jsonl`.
   - Evaluate chunked/streaming audio feature extraction to avoid full-buffer blocking start.

## Notes for next agent
- Keep commit scope on pipeline performance/observability and user-facing status clarity.
- Avoid mixing unrelated UI refactors in same commit if additional changes are needed.
