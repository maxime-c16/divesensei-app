# AI Handoff (2026-04-05)

## Branch
- `feat/mobile-capacitor-scaffold`

## What was done in this session
- Added an audio-first evaluation workflow:
  - `divesensei evaluate-session`
  - extracts and caches `session_audio.wav` once with ffmpeg
  - runs detector from cached WAV instead of re-decoding the source video each time
  - generates a review proxy and writes a dedicated `evaluation` UI session manifest
- Extended the detector to accept pre-extracted audio directly:
  - `detect_from_audio_file(...)`
  - `inspect_audio_proposals_from_audio_file(...)`
- Extended the UI session manifest contract with evaluation-specific fields:
  - `session.mode`
  - `artifacts.source_audio`
  - `artifacts.review_proxy`
  - `artifacts.evaluation_review`
- Added evaluation-mode desktop review behavior:
  - candidate labels: `dive`, `non_dive`, `unsure`
  - manual timeline action: `false_negative`
  - export-oriented copy/actions replaced with evaluation-summary copy/actions
- Added a file-backed evaluation review store in the session output directory:
  - `evaluation_review.json`
  - stores candidate review decisions and false-negative annotations
- Updated desktop review API to branch on session mode:
  - standard sessions still use catalog-backed `keep|reject|unsure`
  - evaluation sessions use file-backed `dive|non_dive|unsure|false_negative`
- Updated desktop catalog bootstrap so terminal-created manifests are imported into the library automatically.

## Validation done
- End-to-end evaluation session completed successfully on:
  - `/Users/mcauchy/Downloads/IMG_8852.MOV`
- Output:
  - `outputs/evaluation_img_8852_20260406-104250`
- Manifest fields validated:
  - `session.mode = "evaluation"`
  - `artifacts.source_audio`
  - `artifacts.review_proxy`
  - `artifacts.evaluation_review`
  - `session.status = "evaluation_ready"`
- Desktop build passed:
  - `cd apps/desktop && npm run build`
- Desktop API validated against the built app preview:
  - saved evaluation candidate decision: `dive`
  - saved manual false negative annotation
  - GET `/api/review` returned both `decisions` and `falseNegatives`
- Fast rerun from cached audio also completed:
  - `outputs/evaluation_img_8852_rerun_20260406-104430`
  - detector started from reused WAV immediately
  - no source-audio re-extraction

## Forge note
- `Forge` remained unstable and repeatedly disappeared mid-run.
- The implementation was validated on a local Downloads file instead.
- If `Forge` becomes stable again, rerun `evaluate-session` there or reuse a cached WAV from a prior successful extraction.

## Pending modified files (now intended to be source-of-truth)
- `apps/desktop/src/lib/analysis-jobs.ts`
- `apps/desktop/src/lib/evaluation-review-store.ts` (new)
- `src/divesensei/cli.py`
- `src/divesensei/app/session_pipeline.py`
- `src/divesensei/detection/audio_detector.py`
- `src/divesensei/detection/config.py`
- `src/divesensei/io/media_io.py`
- `src/divesensei/io/runtime.py`
- `src/divesensei/metadata/ui_contract.py`
- `src/divesensei/profiles.py`
- `src/divesensei/workflows/evaluate_audio_pcen_classifier.py`
- `src/divesensei/workflows/evaluate_session.py` (new)
- `apps/desktop/src/components/DesignWorkspace.astro`
- `apps/desktop/src/lib/ui-data.ts`
- `apps/desktop/src/pages/api/review.ts`
- `apps/desktop/src/types/ui.ts`

## Not included in commit
- `screenshots/` (left untracked intentionally)
- `apps/desktop/package-lock.json` if you do not want to start tracking it

## Suggested next steps
1. Use `evaluate-session` as the main iteration path for detector tuning.
2. Keep reusing `session_audio.wav` for quick reruns while regenerating the proxy only when needed.
3. Decide whether evaluation review data should stay file-backed or move into the desktop catalog DB once the label schema stabilizes.
4. If you want true no-video reruns later, add optional proxy reuse so a fresh evaluation session can be built from cached audio + existing proxy without touching the source video.

## Notes for next agent
- Do not collapse evaluation sessions back into the standard `keep/reject/export` model.
- False negatives are separate manual annotations, not a property of an existing detector candidate.
